[pytest]
# Collection root, so `python -m pytest` from the repo root picks up any new
# tools/**/test_*.py without a workflow edit (LEG-08).
testpaths = tools
python_files = test_*.py
addopts = -ra --strict-markers --strict-config
