# DuneOS /flash/init.yaml for lilygo-t-embed-cc1101
# Services that start at boot, even without an SD card.
services:
  - path: /flash/bin/usb_shell.dap
    restart: always
  - path: /flash/bin/battery_daemon.dap
    restart: always
