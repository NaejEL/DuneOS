# DuneOS /init.yaml for lilygo-t-embed-cc1101
# Services that start at boot, even without an SD card.
services:
  - path: /bin/usb_shell.dap
    restart: always
  - path: /bin/battery_daemon.dap
    restart: always
  - path: /bin/btn_gpio.dap
    restart: always
