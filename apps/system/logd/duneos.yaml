name: logd
version: "0.1.0"
required_abi_version: 1
# FS_READ (32) | FS_WRITE (64) — scans the log dir and rotates/unlinks files.
permissions: 96
# Background housekeeping daemon: small, sleeps most of the time. opendir
# allocates a DIR; 4 KiB is comfortable headroom.
stack_size: 4096
